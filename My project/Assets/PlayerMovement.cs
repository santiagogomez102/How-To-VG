using UnityEngine;

public class PlayerMovement : MonoBehaviour
{
    public Rigidbody rb;
    void Start()
    {
        rb.useGravity = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
